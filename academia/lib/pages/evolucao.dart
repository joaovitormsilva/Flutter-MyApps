import 'package:flutter/material.dart'; 

class EvolucaoPag extends StatelessWidget {
  const EvolucaoPag({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("evolucao", style: TextStyle(color: Colors.blue),),);
  }
}